import React from 'react';
import { Box } from '@adminjs/design-system';

const SetSaida = (props) => {
  return (
    <Box variant="grey">
      <Box variant="white">
        Mesa Liberada
      </Box>
    </Box>
  );
};

export default SetSaida;